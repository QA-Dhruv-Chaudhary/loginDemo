const { test } = require('@playwright/test');

test.describe('Seed / initialization', () => {
  test('project setup placeholder', async () => {
    test.skip(true, 'Add your environment or data setup steps here.');
  });
});
